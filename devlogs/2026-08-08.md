# 2026-08-08

## Fifteenth session - performance regression diagnosed and fixed, via `/sonnet-opus`

The user deployed the fourteenth-session build to their iPad and reported it "super laggy."
`handoffs/NEXT-SESSION-performance-regression.md` (written the same day, previous session) ranked three suspects with file:line pointers: the sniper scope picture-in-picture second render pass, the seven autonomous bots recomputing pathfinding every physics tick, and the High Alert chip's per-opponent raycast.
This session measured before changing anything, then fixed the real causes without reverting any of the three systems, per the user's explicit instruction.

**Measurement.**
Confirmed via code inspection that the scope viewport already resets to `UPDATE_DISABLED` when not scoped (`scripts/duelist.gd:589` at the time) - not a bug, a real cost tradeoff.
Confirmed High Alert's raycast is already gated behind cheap early-outs (team, eliminated, ADS threshold, aim cone) that run before the physics query, in the right order.
Used the existing `_arena_perf_sample` harness (`--offline-squad=<n> --arena-perf-sample`, prints avg/min/max frame time over 120 frames) headlessly on desktop: solo (0 bots) and full-squad (7 bots, 8s into the match so bots were actively pathing, not still holding their opening position) both came back flat at the 120fps cap (`avg_ms=8.33`, zero variance).
That is real signal but a limited one - headless mode never touches the GPU at all, so it can only rule bots/High Alert in or out on CPU, and says nothing about the scope viewport's GPU cost by construction.
Given that, and that mobile CPU/GPU headroom is far tighter than this desktop Mac's, the fix took the "make it cheaper, not free" path on both real suspects rather than declaring either innocent from a desktop-only number.

**Fix 1 - sniper scope (`scripts/duelist.gd`).**
The scope viewport still renders the full scene through `nuclear_pbr` a second time while scoped, but no longer every frame.
It now throttles to `SCOPE_REFRESH_SECONDS` (1/30s) via `SubViewport.UPDATE_ONCE` - a documented Godot 4 mode that renders once and reverts to `UPDATE_DISABLED` automatically - instead of running `UPDATE_ALWAYS` at up to 120Hz.
The scope camera's transform and recoil kick still update every frame, so aiming and recoil read the same; only the actual re-render (and its shading cost) is throttled to roughly a quarter of the previous frequency at 120Hz.
Verified live via MCP `game_eval`: forced ADS/sniper on the local duelist, confirmed `render_target_update_mode` cycles `UPDATE_ONCE -> UPDATE_DISABLED` on schedule with `get_texture()` staying valid throughout, and confirmed it still correctly parks at `UPDATE_DISABLED` when not scoped.

**Fix 2 - autonomous bots (`scripts/bot_duelist.gd`).**
`RiftlineMap.route_toward()` (a per-call walk of every route node against every route blocker/solid when the direct line is obstructed) was being called from `_objective_move_input()` every physics tick, for all seven bots, unconditionally.
It is now cached and recomputed on a `ROUTE_RECOMPUTE_SECONDS` (0.2s) interval, jittered per-bot (`+ randf_range(0.0, 0.08)`) so seven bots don't all recompute on the same tick, with an early recompute trigger if the objective goal itself moves more than 3m since the last route decision.
The steering direction is still derived from the cached waypoint every tick, so a bot's movement input is still smooth and responsive - only the expensive "which via-node to route through" decision is throttled, exactly the fix the diagnosis handoff suggested.

**High Alert - left alone.**
Confirmed its early-outs already short-circuit before the raycast in the right order; no measured or structural reason to change it this session.

**Verification.**
Clean Godot 4.7.1 headless import, all 18 `tools/*_exercise.gd` scripts pass (17 existing plus one added since the fourteenth session's writeup), MCP live main-scene launch reports no runtime errors, and the scope-throttle behavior above was inspected live via `game_eval`, not just read from source.
Not verified on the actual iPad this session - that is the same gap that let the regression ship in the first place, and remains explicitly open. The next real signal on whether this is enough should come from a fresh on-device playtest, not another editor pass.

**Going forward:** the user asked that future development stay optimized for mobile CPU/GPU cost as an ongoing discipline, not a one-off cleanup - see the new "Performance discipline" note below and the standing rule added to this file's own conventions.

## Same day, second round - GPU/CPU thermal diagnosis, physics tick cut, sniper scope rebuilt as FOV zoom

On-device testing surfaced two more real problems the first round's frame-time-only measurement couldn't see: the iPad ran hot even when frame time looked "smooth" (a frame that finishes early on a capped GPU still runs at a high, power-hungry clock - frame-time headroom and absolute work per frame are different things), and the sniper scope was still laggy and now looked "chunky, lower resolution" after the first round's AA-disable fix.

**GPU/CPU thermal diagnosis (`opus-advisor`, Medium - crossed rendering/shader/lighting/project-config systems, exactly the "multi-layer causal reasoning already tried and failed" trigger for an Opus consult).**
Found the real always-on costs: full native iPad resolution at up to 120fps through a 16-hash-per-fragment procedural shader (`shaders/nuclear_pbr.gdshader`), 4-cascade real-time shadows using an expensive PCSS-style filter (`light_angular_distance = 0.6`), and a sky radiance cubemap recomputing every frame with no need to.
Fixed all of it: `rendering/scaling_3d/scale = 0.7` (bilinear), shadow cascades 4→2 with a capped max distance, `light_angular_distance = 0.0` (the single best cost-to-visual-loss ratio of the list), sky baked once via `PROCESS_MODE_QUALITY`, and decorative landmark geometry (light strips, storm-ring bars, pad rings - never a route blocker or gameplay-relevant `_build_solids()` collision body) no longer casts shadows.

**Physics tick 120→60Hz with physics interpolation.**
Verified CoD Mobile's own netcode runs at 30Hz - 120Hz local physics simulation was unusually high by shipped-mobile-shooter norms for no measured gameplay benefit.
Cut to 60Hz; added `SceneTree.physics_interpolation = true` so rendering still runs up to 120fps with no judder, and added `reset_physics_interpolation()` at every genuine teleport (`Duelist.respawn_at()`, a hard `reconcile_from_authority()` snap, the nuclear core returning to center) so those still snap instead of smearing.

**A real infrastructure bug found and fixed along the way.**
`project.godot` text edits turned out to be unreliable in this environment - not just a stale in-memory copy (the existing HANDOFF.md warning), but confirmed capable of silently dropping an entire new section outright the next time the Godot editor opens the project, independent of comment formatting.
Burned real time chasing this as if it were a settings bug before isolating it via `git stash` and fresh isolated test projects.
Fix: moved every performance-critical setting (`physics_ticks_per_second`, `physics_interpolation`, `scaling_3d_scale`/`scaling_3d_mode`) into script (`RiftlineArena._ready()`), which is immune to it since it isn't `project.godot` text. Also confirmed a separate, unrelated headless-capture null-texture error is pre-existing on the unmodified `main` baseline (`git stash` test), not a regression from this session. Both findings are now recorded in `handoffs/HANDOFF.md`'s Tooling section.

**Sniper scope rebuilt as main-camera FOV zoom, picture-in-picture removed entirely (`opus-advisor`, Medium, second consult - user explicitly asked for Opus after two of my own fix rounds still hadn't resolved it).**
The prior round's fixes (30Hz render throttle, then disabling MSAA/TAA) didn't fix "still laggy," and the second one caused a real new regression ("chunky, lower resolution").
Opus's diagnosis, verified by direct citation check: the scope picture was drawn at up to 86% of screen height (`duel_hud.gd`'s `_draw_scope_overlay()`, `radius := lerpf(short_edge * 0.95, target, seated)`) from a 512px source texture - a 2.75-4.4x upscale that no AA setting could fix - while the underlying technique (a second full-scene camera render every throttled frame) was never going to be cheap on a tile-based mobile GPU regardless of tuning.
It also caught a real bug in my own first-round HUD redraw-gate fix: the scope texture was pushed to the HUD unconditionally every physics tick (`riftline_arena.gd`), and the redraw gate checked `_scope_texture != null` - true for any local player at all times, sniper or not - silently defeating the entire "only redraw on change" fix for the whole match.
Removed `Duelist._scope_viewport`/`_scope_camera` entirely; the sniper now zooms the main camera's own FOV via `RiftWeapons.ads_horizontal_fov()`, the exact mechanism every other weapon's ADS already used (the sniper was previously excluded from it specifically to make room for the picture-in-picture design).
`duel_hud.gd`'s tube/vignette art is visually unchanged but now masks the whole screen instead of just its own footprint, since the now-real zoomed view fills the entire screen until masked.
Added magnification-aware ADS look-sensitivity scaling, sniper-only (`DuelHud._ads_magnification()`) - the sniper's 3x/6x zoom is far higher than any other weapon's 1.1-1.5x, and player look input never actually reached a zoomed camera under the old picture-in-picture design, so this was never exercised before.
Removed the now-dead `scope_viewport_texture()`/`set_scope_texture()` plumbing.

**Verification.**
Clean headless import, all 18 exercises pass both after the thermal-diagnosis round and again after the scope rebuild.
Live MCP verification: `Engine.physics_ticks_per_second`/`physics_interpolation` confirmed active via the engine's own interpolation warning firing during `game_eval` testing; both sniper zoom stages (3x/6x) screenshotted live via `game_eval` forcing ADS state, confirmed full-screen mask reaching the true screen edges, correct FOV narrowing, and a crisp full-resolution view with no upscaling artifacts - no runtime errors in either the editor or game logs.
Deployed to the iPad four times across both rounds (two blocked mid-deploy by the device locking, both recovered once unlocked).
**Still not verified on-device by the user for this second round as of this entry** - deployed and launched cleanly, but real on-device feedback (thermal + the scope specifically) is the next real signal, same as every prior round.
## Sixteenth session - mobile ADS drag-look enabled and migrated

The user reported that after opening the sights on mobile they could no longer drag the view onto an enemy and could only reposition by moving the character. The touch router itself was healthy: a free right-side look touch still worked, and `DuelHud._handle_drag()` already had a special path that lets the touch which owns the ADS button also add look delta. The actual fault was configuration: `ads_button_look` defaulted to `false`, and the same false value persisted in existing `user://riftline_controls.cfg` files.

`scripts/duel_hud.gd` now defaults ADS-button drag-look to ON. A new version marker for the controls section migrates pre-versioned installs to ON exactly once; after that migration, a player who intentionally turns `ADS LOOK` OFF in Combat Settings keeps that choice. The existing HOLD/TAP aim mode and separate ADS sensitivity are unchanged.

`tools/riftline_mobile_touch_exercise.gd` now drives the real HUD input seam with two simultaneous `InputEventScreenTouch` owners: the left thumb moves and remains moving, while the right thumb holds ADS, sends `InputEventScreenDrag`, produces sensitivity-scaled look delta, and releases cleanly in HOLD mode. The exercise also proves an old unversioned `ads_button_look=false` config migrates to true and a current versioned explicit false remains false.

Verification: clean Godot 4.7.1 import; all 18 `tools/*_exercise.gd` scripts pass; Godot MCP 3.1.2 launched the live main scene and confirmed the runtime HUD defaults to ADS drag-look ON, a 20/-10 same-touch drag produced 14.4/-7.2 look input at the configured 0.72 ADS sensitivity, and the clean rerun's game log contained no errors. The iOS export was manually signed with Robert's Personal Team (`com.robertwu.whoyoupeekin`), installed and launched on both connected devices, and `devicectl` confirmed a running SOGS process on the iPhone and iPad. Actual touch feel remains a human on-device playtest; this change is event-driven and adds no recurring frame/physics cost.

## Respawn timing and carrier-risk decision

The respawn framing handoff described an older 3.0-second value, while current main actually uses a 5.0-second manual-respawn minimum.
This decision replaces both with one flat 6.0-second minimum.

### Interaction with the 25-second launch

The opposing spawn is about 99.5 metres from an installed launch pad.
At the 7.2 m/s base speed, a straight return takes about 13.8 seconds, so a player killed as the launch begins can spend 6 seconds dead, return in about 19.8 seconds total, and retain about 5.2 seconds to enter the pad and complete the 3-second cancel.
That preserves one narrow second attempt after an immediate death, while a later death can legitimately remove that player from the siege.
The installed team respawns only a few metres from its own pad, so a 6-second absence also gives attackers a meaningful window to begin and complete the 3-second cancel after earning a defensive wipe.
I rejected attacker-specific and defender-specific timers because they would make the same death produce a different hidden penalty according to objective ownership.

### Sudden death

Sudden death uses the same 6.0-second minimum.
Its unbounded clock already prevents the match from expiring before a fair resolution, while the next successful launch makes every life consequential without another rules modifier.
I rejected a longer sudden-death delay because it would turn one elimination into an outsized snowball, and I rejected a shorter delay because it would make sudden-death kills less valuable than normal-play kills.

### Comeback scaling versus competitive legibility

Respawn does not scale with score deficit, time remaining, or the number of dead teammates.
A flat timer lets both teams plan pushes and defensive windows from the same visible rule.
I rejected comeback and wave scaling because hidden assistance would make winning combat feel punished and would obscure whether an opponent can return before a launch or cancel completes.

### Carrier death and the 15-second dropped core

The dead carrier's respawn timer and the dropped-core return timer are independent.
After 6 seconds the player may respawn while an untouched core has about 9 seconds left, but their spawn position and travel distance determine whether they can contest it again.
I rejected tying respawn eligibility to the drop window because doing so would privilege the player who lost the core and weaken the turnover opportunity earned by the elimination.

### Core carry damage

Non-vest carriers again lose 2.5 health per second while carrying, while the Runner's nuclear vest prevents that damage and every carrier keeps the existing 0.82x movement multiplier.
A full-health carrier would require 40 seconds of uninterrupted carrying to die from the core alone, while the direct centre-to-pad route takes roughly 8.7 seconds and costs about 21.8 health.
This makes escorts and the vest meaningful without normally killing a healthy carrier during a direct delivery.
The damage is applied by the authoritative match simulation so client prediction cannot create duplicate or divergent self-damage.

### Implementation and validation

The documentation-only decision commit is `803f109`, and it predates every L2 source edit.
`RiftlineMatch.RESPAWN_MIN_SECONDS` is now 6.0 in the authoritative match, with the respawn timer ticked before core simulation so a carrier eliminated by a one-second damage tick receives no premature timer credit.
`RiftlineMatch.CORE_CARRY_DAMAGE_PER_SECOND` is 2.5, applied only in `_tick_carried()` through `Duelist.take_damage()` for a living carrier without the nuclear vest.
Synchronous carrier defeat returns immediately from `_tick_carried()` after the existing signal path drops the core, leaving the 15-second dropped-core timer independent.
`Duelist._simulate_motion()` remains free of carry damage.
`RiftlineNetwork.PROTOCOL_VERSION` is 13, with no network fields, snapshot keys, event names, or input shapes added or removed.

The focused red-capable mode exercise was made red before the fix: the current five-second constant triggered the explicit six-second assertion, and the authoritative damage expectations were not met.
It passed after the implementation.
The clean headless import passed, and all 18 `tools/*_exercise.gd` scripts passed.
The existing 4v4 headless arena performance sample was `avg_ms=16.67`, `min_ms=16.67`, `max_ms=16.67` before implementation and the same values after implementation over 120 frames.
Headless mode uses a dummy rendering driver, so this CPU sample does not measure mobile GPU cost; the L2 change adds no new render pass or shader work.

Godot MCP 3.1.2 launched the live main scene and verified the death-panel transition, pre-six-second request refusal, six-second eligibility, 2.5 HP/s non-vest damage, Runner immunity, 0.82x carry speed, and protocol 13.
The live game stopped cleanly with no runtime errors; the log contained existing warnings and the expected physics-interpolation warning produced by the eval inspection.
The connected Blender MCP was used only for the required read-only scene-info check, with no Blender changes.
No on-device human playtest was performed in this session.

The unchanged rules were checked in the branch diff: launch countdown 25 seconds, cancel 3 seconds, dropped-core return 15 seconds, post-launch/cancel core respawn delay 4 seconds, first-to-3 scoring, sudden-death launch win handling, spawn selection, and `respawn_started` behavior.

## L1 Competitive Concourse V2 gameplay blockout

The coordinator corrected the scope before implementation, so this session stayed strictly on the Concourse map and did not investigate or change weapon, sniper, HUD, recoil, damage-feedback, or viewmodel systems.

The original `scripts/riftline_map.gd` heuristic layout was replaced by `scripts/riftline_map_layout.gd`, an immutable version 2 layout source with the required solid-record schema, mirrored team transform, gates, pads, spawn pockets, tactical facts, and center, maintenance, and overlook route graphs.
The runtime map now consumes that layout, preserves the arena-facing API, builds collision-only geometry when presentation is disabled, and loads the separate visual scene once when it exists.
Because `res://scenes/concourse_v2_visual.tscn` is not present in this checkout, live and headless presentation runs use the procedural greybox fallback and emit exactly one intentional missing-scene error per map instance.

The collision blockout contains 129 solids and 134 runtime map children in the live presentation path.
It includes the 120m floor cylinder, 40 outer wall segments at 7.2m height, the frozen central towers, baffles, columns, low covers, bridge floor, parapets, and full-height bridge blocker.
Both mirrored bases include the frozen bulkheads, roof, side walls, segmented front walls, blast wall, pad and spawn-pocket screens, approach and offset baffles, side fins, maintenance corridor, overlook deck, alternating cross-baffles, two ramps, and upper connector sightline break.

The bot route contract remains throttled at `ROUTE_RECOMPUTE_SECONDS = 0.2` with per-bot jitter.
Runner uses the center lane, Escort uses maintenance, Defender uses center, and Raider uses overlook.
A carrier delivery intent prefers maintenance, and lane changes invalidate the cached waypoint before the next recompute.
`ai_state()` now exposes the active lane.

The red-capable map exercise was expanded to verify the exact solid schema, team symmetry, route-distance targets, spawn clearance and spacing, tactical anchors, blocker rays, base visibility, bridge-side routes, and upper-level exits.
The bot exercise now verifies role-to-lane mapping, carrier and Escort maintenance preference, cache invalidation, and objective travel through all three authored lane families.

Verification evidence:

- A successful Godot MCP 3.1.2 editor-plugin session was established before investigation and writes through `mcp__godot_ai__session_manage(op="list")`.
- Live MCP `project_run`, `game_eval`, `editor_screenshot`, `editor_manage(op="monitors_get")`, and `logs_read` calls were completed on the final map.
- Live screenshots were checked at base, center, east flank, and upper-overlook poses.
- Live map state reported `solid_count=129`, `map_children=134`, `ambient_motion_count=0`, `map_processing=false` when no pulse was active, and distinct next waypoints for center, maintenance, overlook, and upper-route probes.
- The Godot serial import check passed with no `SCRIPT ERROR`, `Parse Error`, or `Failed to load` matches.
- All 18 `tools/*_exercise.gd` scripts passed after the final edits.
- The direct Godot 4.7.1 offline 4v4 performance sample reported `frames=120 avg_ms=16.67 min_ms=16.67 max_ms=16.67`.
- The mandated serial performance runner again produced an empty log capture, so it is recorded as a runner-output limitation and not as a fabricated result.
- Windowed MCP monitors sampled 308 draw calls and 145 FPS, with physics process around `0.000041-0.000072` seconds and process around `0.001674-0.001759` seconds.
- Final game logs contained only the intentional missing-visual-scene fallback error and the pre-existing camera interpolation warning caused by forcing MCP camera poses; editor logs contained no errors.
- The uncommitted build was exported and signed with the local Apple Development identity, installed to the connected iPad Pro 12.9 (5th generation) with UDID `78C9B3A4-2E79-5827-A287-5F09C7E29ACA`, launched as `com.lull.riftline`, and process-verified through `devicectl` with SOGS PID 2543.

No physical iPhone or iPad human touch or thermal playtest was performed by the agent.
The user subsequently played the completed L1 blockout on iPad and approved its gameplay for commit.
The separate visual scene and detailed device temperature, battery, and long-session frame-pacing confirmation remain outside this commit scope.

## Agent MCP connection gates made mandatory

A verification task interpreted the previous instruction to use Godot MCP for "substantial work" as permission to continue with Godot CLI when the MCP tools were unavailable.
That fallback is no longer permitted.

`AGENTS.md` now makes a successful live `godot-ai` MCP editor-plugin call a hard prerequisite for any implementation, debugging, review, testing, or verification that touches game behavior.
Godot CLI imports, exercises, and performance samples remain complementary checks, not substitutes.
A missing MCP tool surface or editor connection is a blocker, and the task must stop rather than document the limitation and continue.

Blender and 3D-model work now has the same hard gate through an actual successful Blender MCP call.
Any Blender-authored asset intended for the game requires Blender MCP for its source work and Godot MCP for import, integration, runtime, and rendered verification.
Planning, documentation-only changes, and Git or PR administration that do not assess game behavior remain exempt so repository governance can still be repaired when an editor service is unavailable.

This was a documentation-only policy change with no game code, project setting, asset, generated file, runtime behavior, or per-frame cost changed.
